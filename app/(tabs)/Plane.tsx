import React from 'react'
import { SafeAreaView, Text } from 'react-native'

const Plane = () => {
  return (
    <SafeAreaView className='flex-1 justify-center items-center'>
      <Text className='text-white'>Plane</Text>
    </SafeAreaView>
  )
}

export default Plane